<h1><%= @person.name %> has some stuff to do.</h1>

<h2><%= @person.working_tasks.length %> <%= link_to 'tasks', "/people/#{@person.id}/tasks" %> left.</h2>

<%= render 'add_task' %>

<%= render 'completed_tasks_table' %>

<%= render 'people_list' %>