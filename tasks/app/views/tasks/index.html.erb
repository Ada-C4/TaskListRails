<html>
<head>
  <link href='https://fonts.googleapis.com/css?family=Rock+Salt' rel='stylesheet' type='text/css'>
</head>
<body>
    <h1>Task List</h1>
    <div class="center">
      <% @tasks.each do |task| %>
        <p>
          <% if !task.completed.nil?  %>
            <a href="/tasks/<%= task.id %>"><%= task.name %></a>&nbsp;&nbsp;&nbsp;&nbsp;
            <%= button_to "X", "/tasks/#{task.id}", method: :delete, data: { confirm: "Are you sure you want to delete this task?" } %>

          <% else %>
            <a href="/tasks/<%= task.id %>" class="strikethrough"><%= task.name %></a>&nbsp;&nbsp;&nbsp;&nbsp;
            <%= button_to "X", "/tasks/#{task.id}", method: :delete, data: { confirm: "Are you sure you want to delete this task?" } %>
          <% end %>
        </p>
      <% end %>
      <a href="/tasks/new">Add a new task</a>
      </div>
</body>
</html>
<!-- onclick="return confirm('Are you sure? Deleting is permanent!')"> -->
