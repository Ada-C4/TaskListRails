<html>
<head>
  <link href='https://fonts.googleapis.com/css?family=Rock+Salt' rel='stylesheet' type='text/css'>
</head>
<body>
    <h1>Task List</h1>
    <div class="center">
      <% @tasks.each do |task| %>
        <p>
          <% if !task.completed.nil?  %>
            <a href="/tasks/<%= task.id %>"><%= task.name %></a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="http://www.google.com" onclick="return confirm('Are you sure? Deleting is permanent!')">X</a>
          <% else %>
            <a href="/tasks/<%= task.id %>" class="strikethrough"><%= task.name %></a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="http://www.google.com" onclick="return confirm('Are you sure? Deleting is permanent!')">X</a>
          <% end %>
        </p>
      <% end %>
      <a href="/tasks/new">Add a new task</a>
      </div>
</body>
</html>
