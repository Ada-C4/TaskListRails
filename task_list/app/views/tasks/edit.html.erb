<div class="form">

  <%= render "form" %>

</div>

<!-- <%= form_for @task, url: "/tasks/#{@task.id}" do |f| %>
  <fieldset>
    <legend>Task Information</legend>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </fieldset>
  <%= f.submit class: "submit", method: "patch"%>
<% end %> -->
