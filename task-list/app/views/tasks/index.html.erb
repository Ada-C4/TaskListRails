<section>
  <h1>Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
    <li><a href='/tasks/<%= task.id %>'><%= task.name %></a>
    </li>
    <% end %>
  </ul>

  <%= link_to "Create a Task", action: :new %>
  <%= link_to "Remove a Task", action: :remove %>
</section>
