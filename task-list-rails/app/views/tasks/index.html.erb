<h1>My Tasks</h1>
<table>
    <tbody>
      <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= link_to "details", "/tasks/#{task.id}" %></td>
      </tr>
    <% end %>
    </tbody>
</table>
