<%= form_for @task do |f| %>
  <fieldset>
    <legend>task information</legend>
    <%= f.label :name %>:
    <%= f.text_field :name %>

    <%= f.label :description %>:
    <%= f.text_field :description %>

    <%= f.submit %>
  </fieldset>
<% end %>
